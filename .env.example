# ============================================================
# .env.example — Environment Variables for Railway Deployment
# ============================================================
# DO NOT commit a real .env file with actual secrets to GitHub.
# Set these variables directly in the Railway dashboard under
# your service's "Variables" tab.
#
# Railway will automatically provide the MYSQL_* variables
# when you link a MySQL database service to your WordPress
# service. You only need to set the WORDPRESS_* variables.
# ============================================================

# --- Database (Auto-provided by Railway when MySQL is linked) ---
# These are set automatically. Listed here for reference only.
MYSQL_HOST=${{MySQL.MYSQL_HOST}}
MYSQL_PORT=${{MySQL.MYSQL_PORT}}
MYSQL_DATABASE=${{MySQL.MYSQL_DATABASE}}
MYSQL_USER=${{MySQL.MYSQL_USER}}
MYSQL_PASSWORD=${{MySQL.MYSQL_PASSWORD}}

# --- WordPress Database Connection ---
# Railway auto-wires these from the MySQL service variables above.
WORDPRESS_DB_HOST=${{MySQL.MYSQL_HOST}}:${{MySQL.MYSQL_PORT}}
WORDPRESS_DB_NAME=${{MySQL.MYSQL_DATABASE}}
WORDPRESS_DB_USER=${{MySQL.MYSQL_USER}}
WORDPRESS_DB_PASSWORD=${{MySQL.MYSQL_PASSWORD}}

# --- WordPress Site URL ---
# Set this to your custom domain once you've configured it,
# or to your Railway-provided domain initially.
# Example: https://shareyourdreamfriday.com
WORDPRESS_SITE_URL=https://YOUR_DOMAIN_HERE

# --- WordPress Security Keys & Salts ---
# Generate fresh values at: https://api.wordpress.org/secret-key/1.1/salt/
# Paste each value in the Railway Variables tab.
WORDPRESS_AUTH_KEY=REPLACE_WITH_GENERATED_VALUE
WORDPRESS_SECURE_AUTH_KEY=REPLACE_WITH_GENERATED_VALUE
WORDPRESS_LOGGED_IN_KEY=REPLACE_WITH_GENERATED_VALUE
WORDPRESS_NONCE_KEY=REPLACE_WITH_GENERATED_VALUE
WORDPRESS_AUTH_SALT=REPLACE_WITH_GENERATED_VALUE
WORDPRESS_SECURE_AUTH_SALT=REPLACE_WITH_GENERATED_VALUE
WORDPRESS_LOGGED_IN_SALT=REPLACE_WITH_GENERATED_VALUE
WORDPRESS_NONCE_SALT=REPLACE_WITH_GENERATED_VALUE

# --- WordPress Debug (set to false in production) ---
WORDPRESS_DEBUG=false

# --- WordPress Table Prefix (change for security) ---
WORDPRESS_TABLE_PREFIX=wp_
